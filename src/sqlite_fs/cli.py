"""CLI entry point — `sqlite-fs mkfs|mount|umount|fsck ...`.

Stub for v1 development. Regenerated from the engspec package once the
impl specs land (Stage 7).
"""

import sys


def main() -> int:
    sys.stderr.write(
        "sqlite-fs CLI is not yet implemented — v1 is still being specified.\n"
        "See idea.md and plan.md in the repository root.\n"
    )
    return 2


if __name__ == "__main__":
    raise SystemExit(main())
