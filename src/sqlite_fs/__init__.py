"""sqlite-fs — a FUSE filesystem backed by SQLite.

This package is under active development. v1 is being produced via the
engspec methodology: the authoritative sources of truth are `idea.md`,
`plan.md`, and (once generated) the `.engspec` files under `package/`.
The contents of this package below `__init__.py` are regenerated from
those specs; do not edit generated code directly.
"""

__version__ = "0.1.0"
__all__: list[str] = []
